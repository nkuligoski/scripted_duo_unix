DI12U6YF2O0VGZMSS54T
p7H1X8XJ6O61XWuJqhtTo8G7OavnswyYcd4p4VeL
api-d753104b.duosecurity.com


if [ "$common_auth" != "" ]; then
	echo "Configuring /etc/pam.d/sshd with pam_duo.so"
else
	echo "FAILED"
	echo "$common_auth"
fi

if [ -d /lib64/security ]; then
	echo "pam_duo already configured."
else
	echo "Building and installing duo_unix with PAM support"
	./configure --with-pam --prefix=/usr && make && sudo make install
fi


sed -i '/![^#]/ s/\(.*pam_unix.*$\)/#\1/' /etc/pam.d/common-auth